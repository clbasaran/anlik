<?xml version="1.0" encoding="utf-8"?>
<paths>
    <cache-path name="shared_photos" path="/" />
    <!-- Self-hosted in-app updates: APKs land in
         getExternalFilesDir(Environment.DIRECTORY_DOWNLOADS) and the system
         installer needs a content:// URI to read them. -->
    <external-files-path name="apk_updates" path="Download/" />
</paths>
